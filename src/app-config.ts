import { ShiftHours } from "./types/shift-hours";

// DEPRECATED: dayStart is now managed by the GAPI store and loaded from settings sheet H1
// Keep this export for backward compatibility
export const dayStart: ShiftHours = "14:00";